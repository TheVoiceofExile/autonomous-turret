const assert = require('chai').assert;

// TODO: export functions from bot.js
const bot = require('../bot'); 

describe('bot', function () {
    it('should <do something>', function () {
        // Test statement (assertion)
    });
});
